# Full install (core + audio pipeline) — equivalent to the old flat file, so
# `pip install -r requirements.txt` still installs everything.
#
# For a lightweight, library-only install without Whisper/torch (~2-3 GB
# smaller), use:   pip install -r requirements-core.txt
#
# Easiest path on any OS: run `python install.py` (or double-click
# install.bat / install.command), which detects your platform, lets you pick
# core vs full, and installs the optional ffmpeg / 7-Zip tools for you.

-r requirements-pipeline.txt
